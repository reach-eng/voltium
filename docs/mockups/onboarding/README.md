# Onboarding mockups

These HTML mockups were originally at the workspace root
(`D:\voltium\onboarding-flow-*.html`) and were relocated here as part
of ONBOARDING-AUDIT 2026-08-14 (P2-14). They are reference designs for
the active onboarding path (phone → OTP → userForm → guarantor →
plan → deposit → pickup → dashboard).

- `onboarding-flow-mockup.html` — early design pass (98 KB).
- `onboarding-flow-redesign.html` — later redesign (130 KB).

Neither file is referenced from any code. They are kept here for the
user-facing team to reference; if no longer needed, delete the
directory.
