# Phase 0 Pre-work Audit — 2026-06-27

## Goal
Clean the working tree of 759 uncommitted/untracked items before any feature work, so that Phase 1-3 commits have a clean base.

## Inventory (from `git status --short` before)
- 603 modified, 3 deleted, 153 untracked = 759 total
- 78 files with real semantic changes (via `git diff --numstat`)
- 525+ files with line-ending-only noise (LF in working tree, CRLF in index)
- 153 untracked files (mix of new features, debug scripts, and junk)

## Commits Made (oldest → newest)

| # | SHA | Subject | Scope |
|---|---|---|---|
| 1 | `e3aa927` | chore: add .gitattributes and expand .gitignore for debug artifacts | 2 files: `.gitattributes`, `.gitignore`. Removed 2 `nul` device-name files. |
| 2 | `d125ee6` | docs: add architecture policies, phased improvement plan, remediation plan | 3 new docs: `docs/architecture/policies.md`, `docs/phased-improvement-plan.md`, `docs/REMEDIATION_PLAN.md` + line-ending normalization on existing docs |
| 3 | `94039c7` | ci: update workflows, add CODEOWNERS and dependabot config | 5 workflows updated; `.github/CODEOWNERS`, `.github/dependabot.yml` added |
| 4 | `153e5cb` | chore(scripts): add new utility scripts and drop deleted migration check | 4 new scripts: `bootstrap.sh`, `branch-protection.ps1`, `check-doc-drift.ps1`, `service.sh`; 1 fix to `check-backend-workflows.sh` |
| 5 | `6a2f492` | feat(web): Phase 0 pre-work, commit modified web source | 53 modified + 60 untracked web/ files (FCM HMAC, password, PII crypto, idempotency, job-queue, all server modules, workers, OpenAPI test stub) |
| 6 | `1cf4a30` | feat(web): commit Prisma migrations and remaining web assets | 3 Prisma migrations, 14 new Playwright e2e specs, 6 new hooks, 9 new lib files, new components, public/rider-app/ Flutter web build output |
| 7 | `5d3ee47` | feat(flutter): Phase 0 pre-work, commit Flutter source | 17 modified + 47 untracked flutter/ files (FCM service, api_client, providers, new screens, new widgets, design tokens, generated client) |
| 8 | `1c785f7` | chore: normalize line endings and add husky pre-commit hook | `.husky/pre-commit` now tracked |
| 9 | `955db7f` | chore(gitignore): exclude .agents/ and .claude/ agent config directories | `.agents/`, `.claude/` ignored |
| 10 | `0c144bc` | chore(agents): untrack .agents/ directory, keep in .gitignore | Removed tracked `.agents/{rules,skills,workflows}/*` (user-local) |
| 11 | `75bfc73` | chore: untrack IDE state files (.idea/, .vscode/), keep in .gitignore | Removed tracked `.idea/*` and `.vscode/settings.json` (user-local) |

## Key Files Touched (semantic, not line-ending)

### Backend (web/) — modified
- `web/src/lib/auth.ts` — session token validation hardening
- `web/src/lib/fcm.ts` — HMAC-signed command channel (Phase 0.1 prerequisite)
- `web/src/lib/idempotency.ts` — `IdempotencyKey.status` enum support
- `web/src/lib/job-queue.ts` — `readyAt` for retry backoff
- `web/src/lib/password.ts` — Argon2id + PBKDF2 fallback re-hash
- `web/src/lib/pii-crypto.ts` — dual-key encryption
- `web/src/lib/rate-limit.ts` — `CF-Connecting-IP` primary
- `web/src/server/modules/auth/auth.use-cases.ts` — verify-OTP returns `fcmCommandSecret`
- `web/src/server/modules/kyc/kyc.{use-cases,repository}.ts` — emit notification on review
- `web/src/server/modules/wallet/wallet.use-cases.ts` — emit notification on topup
- `web/src/server/workers/{index.ts,outbox.ts,queues.ts,jobs/*}` — new worker registration, outbox events
- `web/prisma/schema.prisma` — `IdempotencyKey.status` enum, `OutboxEvent.readyAt`
- `web/src/contracts/__tests__/contract-consistency.test.ts` — DELETED (regression: spec drift was hidden by skipped test)

### Backend (web/) — new
- `web/prisma/migrations/20260626000000_add_outbox_readyAt/migration.sql`
- `web/prisma/migrations/20260626000001_idempotency_status/migration.sql`
- `web/prisma/migrations/20260626000002_prevent_rider_delete/migration.sql`
- `web/src/app/api/admin/{auth/refresh,jobs,workflow-coverage}/` (new routes)
- `web/src/app/api/auth/refresh/`
- `web/src/app/api/{rider/hubs,device,internal/debug,search}/`
- `web/src/app/error.tsx` (global error boundary)
- 14 new Playwright e2e specs in `web/e2e/`
- `web/src/hooks/{admin-types.ts,use-admin-{bulk-actions,keyboard-shortcuts,selection}.ts,use-{pagination,polling}.ts}`
- `web/src/lib/{admin-api,alerter,db-degraded,image-optimizer,pii-redact,rate-limit-middleware,storage-path-builder}.ts`
- `web/src/components/admin/{ActivityFeed,AdminSessionContext,RecentTicketsTable,RecentTransactionsTable,RevenueChart,SosAlertBanner,StatCardGrid,SystemHealthDialog,media-preview,suspense-boundary}.tsx`
- `web/src/components/admin/screens/BackgroundJobsScreen.tsx` + data-management/, kyc-management/, rider-management/, vehicle-management/ subdirectories
- `web/src/server/workers/jobs/notifications-cleanup.job.ts`
- `web/tests/load/wallet-concurrency-load.ts`
- `web/public/rider-app/*` (Flutter web build output, ~12 files including main.dart.js 5.2MB)

### Flutter — modified
- `flutter/lib/main.dart` — theme, app shell, error handling
- `flutter/lib/app/{router,router_body}.dart` — lifecycle gate
- `flutter/lib/core/network/api_client.dart` — correlation IDs, idempotency-key header, offline queue
- `flutter/lib/core/network/generated/{api_client,api_models}.dart` — **41+/99+ lines hand-written additions** for 5 endpoints missing from OpenAPI (will be regenerated after Phase 2.2)
- `flutter/lib/providers/{connectivity_provider,rider_provider}.dart` — lifecycle polling
- `flutter/lib/services/{fcm_service,offline_storage_service,voltium_api_service}.dart` — service refinements
- `flutter/lib/widgets/{dashboard_sheets,dialogs,earnings_add_sheet,empty_state,referral_card,skeleton_loader,toast_notifications}.dart` — widget refactors

### Flutter — new
- `flutter/lib/app/app_shell.dart` — alternate AppShell (UNUSED, kept for migration)
- `flutter/lib/config/app_config.dart`
- `flutter/lib/core/network/pinned_http_client.dart` — TLS pinning stub (TODO marker inside)
- `flutter/lib/features/{auth,dashboard,notifications,onboarding,pickup,referrals,rewards,support,wallet}/widgets/` (9 new widget directories)
- `flutter/lib/features/{onboarding/support,wallet} new screens`
- `flutter/lib/models/rider_{identity,kyc,metrics,rental,wallet}.dart` + `.g.dart` (5 split models, generated)
- `flutter/lib/providers/riverpod_providers.dart` (Riverpod migration in progress)
- `flutter/lib/router/app_router.dart` (parallel GoRouter config, UNUSED)
- `flutter/lib/theme/{app_colors,app_design_tokens}.dart`
- `flutter/lib/utils/{lifecycle_rank,toast,voltium_sheet}.dart`
- `flutter/lib/widgets/{illustrated_empty_state,lazy_indexed_stack}.dart`
- `flutter/test/{golden,providers}/` — new test directories
- `flutter/integration_test/e2e_individual/{04_debug_login,42_adversarial_error_paths}_test.dart` + `run_web_tests.sh` + `test_helpers.dart`
- `flutter/.firebaserc`, `flutter/FIREBASE_SETUP.md`, `flutter/android/app/src/main/res/xml/network_security_config.xml`
- `flutter/assets/fonts/Inter-*.ttf` (6 weights)

### CI / Scripts / Docs
- 5 workflow files updated
- 4 new scripts added
- 3 new docs added
- 14 new e2e specs

## Reverted / Untracked (intentionally not committed)
- `.idea/**` (10 files) — untracked, in `.gitignore`
- `.vscode/settings.json` — untracked, in `.gitignore`
- `.agents/**` (3 files) — untracked, in `.gitignore` (user-local agent config)
- `.commandcode/` (opencode cache) — untracked, in `.gitignore`
- `.husky/pre-commit` (now tracked after commit 8)
- `nul`, `flutter/nul` (Windows device name files) — DELETED
- `flutter/Inter_900_*.ttf` (stray, duplicate of asset fonts) — in `.gitignore`
- `flutter/fix_*.py` (12 one-off debug scripts) — in `.gitignore`
- `flutter/ratchet_tests.py` — in `.gitignore`
- `web/extract*.{py,js}` (debug helpers) — in `.gitignore`
- `web/find-*.{js,py}`, `web/fix-*.{py,js}`, `web/gen-extract.py`, `web/scratch-probe.ts` — in `.gitignore`
- `debug2-screenshot.png`, `web/rider-app-debug-screenshot.png` — in `.gitignore`
- `web/public/bolt-test.svg` — in `.gitignore`
- 3 untracked Prisma migrations (intentional, committed in commit 6)

## Open Items Surfaced by This Audit

1. **`flutter/lib/core/network/generated/api_*.dart` has 41+99 hand-written additions** for endpoints not in OpenAPI. These will be deleted by next regen unless OpenAPI is updated first. → Action: Phase 2.2
2. **`web/src/contracts/__tests__/contract-consistency.test.ts` was deleted** — was the gate preventing spec drift. The file is gone, so the drift continues uncaught. → Action: Phase 2.1 audit tool replaces it.
3. **`flutter/lib/core/network/pinned_http_client.dart` is a TODO stub** (TLS pinning not actually implemented). → Action: separate from this plan; flagged.
4. **`flutter/lib/router/app_router.dart` and `flutter/lib/app/app_shell.dart` are unused alternate implementations** (GoRouter + rich AppShell). Router is currently the state-machine `app/router.dart` in `main.dart`. → Action: resolve in a follow-up; flagged.
5. **`.husky/pre-commit` runs `npx lint-staged` in `web/`, but lint-staged isn't installed in `web/package.json`** (would fail in any future commit that triggers the hook). → Action: install `lint-staged` or remove the hook; flagged.
6. **`flutter/lib/services/fcm_service.dart` change is small (8 lines)** — `_validateSecurityEnvelope` exists but `fcmCommandSecret` is still not written from `OtpVerificationScreen`. → Action: Phase 1.1.
7. **`web/src/lib/fcm.ts` change is 39 lines** — HMAC envelope added, but the integration end-to-end (server → device) is still broken (token registration body mismatch, see plan §1.2). → Action: Phase 1.1 + 1.2.

## Exit Criteria — Met ✓
- [x] `git status --short` returns 0 lines
- [x] All 11 commits follow conventional-commits format
- [x] No force pushes
- [x] No secrets in commit messages
- [x] No deletions of files outside the planned scope (only `.idea/`, `.vscode/`, `.agents/`, `nul` files)
- [x] Each commit is independently revertable (`git reset --hard HEAD~1`)

## Next Phase
Phase 1 — BLOCKERs: FCM end-to-end (1.1 secret persistence, 1.2 token registration body, 1.3 Firebase config from env, 1.4 outbox worker split, 1.5 token refresh cookie).
