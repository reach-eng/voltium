# Voltium Local Development Overrides
# Copy this to .env.local for local-only overrides not committed to git.

# Override the database to use local PostgreSQL
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/voltium-dev?schema=public"

# Enable local dev tools
# ENABLE_DEV_TOOLS=true
