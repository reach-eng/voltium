/// App-wide configuration constants.
///
/// F-26: Re-export canonical `AppConfig` from `package:voltium_rider/config/app_config.dart`
/// to eliminate duplicate class definitions and ensure a single source of truth across
/// the codebase for both environment settings and support/legal constants.
library;

export 'package:voltium_rider/config/app_config.dart';
