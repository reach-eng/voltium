{
  "name": "voltium-root",
  "private": true,
  "scripts": {
    "prepare": "husky install .husky",
    "check:tokens": "npx tsx scripts/sync-tokens.ts"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0",
    "npm": ">=10.0.0"
  }
}
